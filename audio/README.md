# Audio folder

This folder is intentionally empty.

The production app does not require audio to be committed to GitHub. Open the app and use **Choose audio files**; it will decode each MP3/M4A/WAV/AAC file, generate constellation fingerprints, store the fingerprints in IndexedDB, and discard the decoded audio.

Keeping copyrighted recordings out of a public GitHub repository is recommended.
